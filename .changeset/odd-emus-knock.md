---
"@nmi-agro/fdm-core": minor
---

Farm roles are now provided as a list of objects with keys `role`, `principal_id`, and `principal_type` ("user" or "organization") in order to provide more context to the user.
