---
"@nmi-agro/fdm-docs": patch
---

Migrate to TypeScript V7. However keep TypeScript V6 for typedoc as V7 does not expose the API that typedocs needs (will probably be included in TypeScript V7.1).
