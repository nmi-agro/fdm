---
"@nmi-agro/fdm-app": patch
---

Opt in to stable React Router v8 future flags to suppress startup warnings: `v8_splitRouteModules`, `v8_viteEnvironmentApi`, `v8_passThroughRequests`, and `v8_trailingSlashAwareDataRequests`
